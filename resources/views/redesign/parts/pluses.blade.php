{{-- <div class="main-pluses carousel slide" id="mainPlusesCarousel" data-ride="carousel">
	<ol class="carousel-indicators">
		@foreach($pluses as $index => $plus)
			<li data-target="#mainPlusesCarousel"
				data-slide-to="{{$index}}"
				class="{{$index === 0 ? 'active' : ''}}"></li>
		@endforeach
	</ol>

	<div class="carousel-inner">
		@foreach($pluses as $index => $plus)
			<div class="carousel-item {{$index === 0 ? 'active' : ''}}">
				<div class="plus">{{$plus}}</div>
			</div>
		@endforeach
	</div>
</div> --}}